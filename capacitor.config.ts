import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.frigg.formulaleague',
  appName: 'formulaLeagueApp',
  webDir: 'www'
};

export default config;
